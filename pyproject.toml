[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "FeSim"
version = "0.1.0"
description = "Fire Emblem character growth simulator"
requires-python = ">=3.13"

[tool.setuptools.packages.find]
where = ["src"]